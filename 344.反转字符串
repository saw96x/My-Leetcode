#include <vector>
using namespace std;
class Solution
{
public:
    void reverseString(vector<char> &s)
    {
        int front = 0, back = s.size() - 1;
        while (back > front)
            swap(s[front++], s[back--]);
    }
};
